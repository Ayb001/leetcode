In this solution, I used the brute force approach in C to solve the Two Sum problem.

The idea is to compare each element in the array with the rest of the elements until finding two numbers whose sum equals the target value. Once the correct pair is found, their indices are returned.

This approach is simple and easy to understand, but it is not the most optimized solution because it uses nested loops, resulting in a time complexity of O(n²).
