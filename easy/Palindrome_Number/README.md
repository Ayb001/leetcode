# Palindrome Number

This solution checks whether a number is a palindrome without converting it to a string.

I reversed the number using modulo and division operations, then compared the reversed number with the original value. If both are equal, the function returns `true`; otherwise, it returns `false`.

Language used: C

