# Roman to Integer

This solution converts a Roman numeral into an integer using the Roman numeral rules.

The algorithm traverses the string and compares the current symbol with the next one. If the current value is smaller, it subtracts it; otherwise, it adds it normally.
